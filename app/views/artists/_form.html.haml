= form_for(@artist, html:{ class: "pure-form pure-form-aligned" }) do |f|
  %fieldset
    - if @artist.errors.any?
      #error_explanation
        %h2
          = pluralize(@artist.errors.count, "error")
          prohibited this artist from being saved:
        %ul
          - @artist.errors.full_messages.each do |message|
            %li= message
    .pure-control-group
      = f.label :name
      = f.text_field :name, placeholder: "Artist Name"
    .pure-control-group
      = f.label :rating
      = f.number_field :rating, in:0..5, placeholder: "0 to 5"
    .pure-controls
      = f.submit class: "pure-button pure-button-primary"
