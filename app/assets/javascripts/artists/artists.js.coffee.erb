angular.module('artists', ['ngRoute', 'ngResource'])
.config(['$routeProvider', ($routeProvider) ->
  $routeProvider
  .when('/artists', {
    templateUrl: '<%= asset_path("artists/index.html") %>',
    controller: 'ArtistsIndexController'
  })
])
